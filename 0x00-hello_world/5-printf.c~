#include <stdio.h>

/**
 * main - entry points
 * 
 * Descriptions: Output sentence using printf
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
